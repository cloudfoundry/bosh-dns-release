#!/bin/bash -exu

/var/vcap/jobs/bosh-dns/bin/wait

dig +tcp +time=10 +tries=1 -p <%= p('port') %> <%= p('upcheck_domains').first %> @<%= p('address') %>
dig +notcp +time=10 +tries=1 -p <%= p('port') %> <%= p('upcheck_domains').first %> @<%= p('address') %>
